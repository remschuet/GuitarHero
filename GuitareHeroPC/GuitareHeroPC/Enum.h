#pragma once

enum NotesEtats {
	AFFICHER,
	EN_ATTENTE
};

enum CouleurBouton {
	ROUGE,
	VERT,
	BLEU,
	JAUNE,
	MAUVE,
	UNKNOWN
};

enum NotesAction {
	APPUYE,
	RELACHE,
	INITIALE
};

enum ComMode {
	BLUETOOTH,
	FILAIRE,
	CLAVIER
};